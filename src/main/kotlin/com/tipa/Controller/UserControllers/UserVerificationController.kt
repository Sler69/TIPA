package com.tipa.Controller.UserControllers

import spark.Request
import spark.Response


class UserVerificationController {
   companion object {
       fun findUsername(req: Request, resp: Response): Any {
           return 0
       }

       fun findEmail(resp: Response, req: Request): Any {
           return 0
       }
   }
}
